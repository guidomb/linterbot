require 'spec_helper'

describe Linterbot do
  it 'has a version number' do
    expect(Linterbot::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
